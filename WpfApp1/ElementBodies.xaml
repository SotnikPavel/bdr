<Window x:Class="WpfApp1.ElementBodies"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Корпуса" Height="300" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="18"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Menu x:Name="menu" Grid.Row="0">
            <MenuItem Header="Добавить" Click="MenuItem_Click"/>
            <MenuItem Header="Изменить"/>
            <MenuItem Header="Удалить"/>
            <MenuItem Header="Типы эл."/>
        </Menu>
        <ListView x:Name="listView" Grid.Row="1" ItemsSource="{Binding .}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="120" Header="Тип" DisplayMemberBinding="{Binding Path=Name}" />
                    <GridViewColumn Width="120" Header="Описание" DisplayMemberBinding="{Binding Path=Description}"/>
                    <GridViewColumn Width="95" Header="Кол-во выводов" DisplayMemberBinding="{Binding Path=PinsQuantity}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
