<Window x:Class="WpfApp1.ComponentStorages"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Места хранения компонента" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="18"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Menu x:Name="menu" Grid.Row="0">
            <MenuItem  Name ="Add" Header="Добавить" Click="MenuItem_Click"/>
            <MenuItem Name="Change" Header="Изменить" Click="MenuItem_Change"/>
            <MenuItem Name="Delete" Header="Удалить" Click="MenuItem_Delete"/>
        </Menu>
        <ListView x:Name="listView" Grid.Row="1" ItemsSource="{Binding .}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="120" Header="Название" DisplayMemberBinding="{Binding Path=Name}" />
                    <GridViewColumn Width="120" Header="Количество" DisplayMemberBinding="{Binding Path=Count}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
