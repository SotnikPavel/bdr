<Window x:Class="WpfApp1.TypeElementClassesBaseClassSelect"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Выберете базовый тип элементов" Height="300" Width="300">
    <Grid>
        <TreeView ItemsSource="{Binding Path=.}" SelectedItemChanged="TreeView_SelectedItemChanged">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Path=ElementClassesData}" >
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Название: "></TextBlock>
                        <TextBlock FontWeight="Bold" Text="{Binding Name}"></TextBlock>
                        <TextBlock Text=" Описание: "></TextBlock>
                        <TextBlock FontWeight="Bold" Text="{Binding Description}"></TextBlock>
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</Window>
